# 后端 API：pip install -r requirements-web.txt && python web/app.py
# 前端：cd frontend && npm install && npm run dev
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
requests>=2.32.0
python-dotenv>=1.0.0
Pillow>=10.0.0
